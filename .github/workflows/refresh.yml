name: refresh-hub

on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 min (GitHub free-tier schedules can lag/skip under load)
  workflow_dispatch: {}       # lets you run it manually from the Actions tab

permissions:
  contents: write             # allow the job to commit the updated data.json

concurrency:
  group: refresh-hub
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: python collector.py

      - name: commit updated data
        run: |
          git config user.name  "media-hub-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data.json
          if git diff --staged --quiet; then
            echo "no changes"
          else
            git commit -m "hub: refresh $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          fi
